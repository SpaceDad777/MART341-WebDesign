The advantages of using a text editor is that it is faster to edit, make quick and easy changes to a page, or to add or remove characters. 
Both of them have their advantages and disadvantages. Relative URL only includes the path. They are also more convienant than absolute, they tend to carry more information. Absolute URL also makes is portable and shoter. It is used as a resource URL. While as absolute uses the full URL, containing the protocol, sub domain, and the path. While it may be a length URL, it provides all the information to find the page or resource you are looking for. 